#default parameter (we can pass the dictionary in function parameter)

def fun(name = "unknown", age = 23):
    print(name,age)#it prints simple age and name seprated by
fun()
fun("harshit",20)#if we pass the arguement in called function so its overwrite the values    